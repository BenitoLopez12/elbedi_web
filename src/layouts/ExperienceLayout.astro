---
// Layout del index cinematográfico. A diferencia de /websites, este layout
// usa un documento vertical real: Lenis suaviza el scroll nativo y GSAP lo
// convierte en la línea de tiempo narrativa.
import "../styles/app.css";
import "../styles/experience.css";
import Analytics from "@vercel/analytics/astro";
import SpeedInsights from "@vercel/speed-insights/astro";
import siteConfig from "../config/siteConfig.js";
import Head from "./partials/head.jsx";
import Preloader from "./partials/Preloader.astro";

const currentCanonical = Astro.url
  ? new URL(Astro.url.pathname, siteConfig.url).toString()
  : siteConfig.url;

const {
  title = siteConfig.title,
  description = siteConfig.description,
  canonicalUrl = currentCanonical,
  ogImage = siteConfig.mainImage,
  ogImageAlt = siteConfig.mainImageAlt,
  robots = siteConfig.robots,
  schema = [],
} = Astro.props;
---

<!doctype html>
<html lang={siteConfig.lang} class="dark cine-experience">
  <Head
    title={title}
    description={description}
    canonicalUrl={canonicalUrl}
    ogImage={ogImage}
    ogImageAlt={ogImageAlt}
    robots={robots}
    schema={schema}
  />
  <body class="text-white antialiased">
    <a class="global-skip-link" href="#main-content">Ir al contenido</a>
    <Preloader />
    <slot />
    <Analytics />
    <SpeedInsights />
  </body>
</html>
